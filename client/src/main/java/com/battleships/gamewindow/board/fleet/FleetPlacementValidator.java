package com.battleships.gamewindow.board.fleet;

import com.battleships.gamewindow.board.BoardSize;

public class FleetPlacementValidator {
    private final BoardSize boardSize;

    public FleetPlacementValidator(BoardSize boardSize) {
        this.boardSize = boardSize;
    }


    public void validateFleet(int [][][] arrayToCreateFleet) {
        int length = arrayToCreateFleet.length;
    }
}
